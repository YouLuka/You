const TOTAL_IMAGES=29;const EXTENSION='jpg';const gallery=document.querySelector('.gallery');const images=[];for(let i=1;i<=TOTAL_IMAGES;i++){const img=document.createElement('img');img.src=`images/${i}.${EXTENSION}`;img.alt=`Photo ${i}`;img.loading='lazy';img.onclick=()=>openBox(i-1);gallery.appendChild(img);images.push(img.src);}const box=document.getElementById('lightbox');const big=document.getElementById('lightbox-img');let current=0;function openBox(i){current=i;big.src=images[current];box.classList.add('show')}function update(){big.src=images[current]}document.querySelector('.close').onclick=()=>box.classList.remove('show');document.querySelector('.prev').onclick=()=>{current=(current-1+images.length)%images.length;update()};document.querySelector('.next').onclick=()=>{current=(current+1)%images.length;update()};document.addEventListener('keydown',e=>{if(!box.classList.contains('show'))return;if(e.key==='Escape')box.classList.remove('show');if(e.key==='ArrowLeft')document.querySelector('.prev').click();if(e.key==='ArrowRight')document.querySelector('.next').click();});